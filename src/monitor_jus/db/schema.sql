-- Schema de referência (criado automaticamente via SQLAlchemy Base.metadata.create_all)
-- Dual: SQLite (dev) / PostgreSQL (prod)

-- Ver models.py para definição completa das tabelas:
-- criteria, provider_subscriptions, webhook_deliveries, webhooks_raw,
-- runs, jobs, job_dead_letter, processes, process_movements, communications,
-- events, event_versions, event_quarantine, criterion_links,
-- source_capabilities, source_checkpoints, ai_generations,
-- digests, digest_items, digest_cursor, notifications, delivery_attempts,
-- audit_log, execution_locks, bootstrap_state
